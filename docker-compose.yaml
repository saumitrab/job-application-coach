
services:
  frontend:
    build: frontend
    ports:
      - 8501:8501
    depends_on:
      - backend
    volumes:
        - ./frontend:/app

  backend:
    build: backend
    expose:
      - "7860"
    ports:
      - 7860:7860
    volumes:
      - ./backend:/app
